int	*top_swap(int arr[])
{
	int	tmp;

	tmp = arr[0];
	arr[0] = arr[1];
	arr[1] = tmp;
	return (arr[]);
}

int	*top_push(int taker[], int giver[])
{
	int	tmp;

	if (giver[0] == '\0')
		return (NULL);
	while ()
	{
		tmp = taker[i];
		if (i == 0)
			taker[i] = giver[0];
	}

}
